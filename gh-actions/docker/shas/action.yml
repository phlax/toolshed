inputs:
  images:
    type: string
    required: true
  env_prefix:
    type: string
    default: OUTPUT
outputs:
  shas:
    value: ${{ steps.images.outputs.shas }}

runs:
  using: composite
  steps:
  - uses: envoyproxy/toolshed/gh-actions/jq@ced9da8529d19e8a4f687e94eaabc2a6130d987e
    id: yaml
    with:
      input: ${{ inputs.images }}
      input-format: yaml
  - uses: envoyproxy/toolshed/gh-actions/jq@ced9da8529d19e8a4f687e94eaabc2a6130d987e
    id: json_actions
    with:
      input: ${{ steps.yaml.outputs.json }}
      filter: |
        {runs: {using: "composite", steps: [to_entries[] | {
          uses: "envoyproxy/toolshed/gh-actions/docker/sha@ced9da8529d19e8a4f687e94eaabc2a6130d987e",
          id: .key,
          with: {
            image: .value,
            env_var: "${{ inputs.env_prefix }}_\(.key)",
        }}]}}
  - run: |
      mkdir -p .tmp.action
      echo '${{ steps.json_actions.outputs.value }}' > .tmp.action/action.yml
    shell: bash
  - name: Run (.tmp.action)
    uses: ./.tmp.action
  - name: Cleanup (.tmp.action)
    if: always()
    shell: bash
    run: rm -rf .tmp.action
